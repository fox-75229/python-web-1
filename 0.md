## 專案簡報講稿：Flask 心臟病預測平台（網頁演示視角）

（*預計時間：約 3-5 分鐘*）

---

### 1. 開場與首頁導覽 (約 60 秒)

大家好，今天我要帶各位實際瀏覽我們開發的「**Flask 心臟病預測平台**」。

**(情境：假設瀏覽器開啟 http://127.0.0.1:5000)**

首先，當我們啟動伺服器並進入首頁時，映入眼簾的是我們的主畫面。
大家可以看到，這裡有一個清晰的導覽列（Navigation Bar），這是由我們的基礎模板 `base.html` 所定義的，確保了所有頁面的一致性。

首頁的核心目的是**引導使用者**。我們在這裡簡述了專案的目標——透過機器學習來預測心臟病風險。使用者不需要懂程式碼，只要透過這個網頁介面，就能使用背後強大的 AI 模型。

在導覽列上，我們提供了兩條主要的路徑，分別對應專案的兩個核心模型：**「邏輯斯迴歸預測」** 與 **「決策樹預測」**。

### 2. 功能演示：輸入與互動 (約 60 秒)

接下來，讓我們以 **「邏輯斯迴歸」** 頁面為例進行操作。

當使用者點擊進入後，會看到一個結構化的表單。
這個介面是由 HTML 搭配 CSS 樣式渲染而成（位於 `templates/logistic.html`）。我們設計了直觀的輸入欄位，讓使用者填寫關鍵的健康數據，例如：
* 年齡
* 性別
* 膽固醇指數
* 血壓...等

**(情境：填寫完畢，按下「預測」按鈕)**

當使用者填寫完畢並按下「預測」按鈕時，前端的 JavaScript 會攔截這個請求，將數據打包並發送到我們的 Flask 後端 (`app.py`)。

### 3. 幕後流程：後端處理與預測 (約 45 秒)

在這個等待的瞬間（通常不到一秒），後端正在執行關鍵的任務：

1.  **接收數據**：Flask 的 `/logistic` 路由接收到前端傳來的表單資料。
2.  **數據轉換**：將使用者的輸入轉換成模型看得懂的格式（例如將類別轉為數值）。
3.  **模型推論**：呼叫我們預先訓練好的 `Logistic Regression` 模型進行計算。
4.  **回傳結果**：模型計算出「患病機率」或「分類結果」，再回傳給前端頁面進行渲染。

這整個過程，展現了我們如何將 Python 資料科學的成果，透過 Web 框架落地應用。

### 4. 模型選擇與差異說明 (約 90 秒)

您可能會好奇，為什麼我們要在導覽列提供 **兩個不同的預測頁面**？這正是本專案的技術亮點。

我們希望讓使用者或開發者能比較不同演算法的特性：

* **如果使用者選擇「邏輯斯迴歸 (Logistic Regression)」頁面**：
    * 背後運行的是一個**線性模型**。
    * 它的優勢在於**穩定且具解釋性**。就像醫生看診一樣，我們可以很清楚知道是哪一個指標（例如膽固醇過高）拉高了風險機率。這適合用於初步篩選或需要明確解釋原因的場景。

* **如果使用者選擇「決策樹 (Decision Tree)」頁面**：
    * 背後運行的是一個**非線性模型**。
    * 它的邏輯像是一連串的「如果是 A，則走 B」的樹狀判斷。它的強項在於捕捉數據中更複雜、非線性的關聯。雖然它可能更精準，但也相對容易受到數據微小變動的影響。

透過網頁上的這兩個選項，我們實作了模型的 A/B 測試概念，展示了不同演算法在同一組數據下的表現。

### 5. 結尾 (約 30 秒)

最後，我們回到網頁介面。

這個專案不僅僅是一個預測工具，它整合了 **前端的互動體驗 (HTML/JS)**、**後端的邏輯處理 (Flask)** 以及 **資料科學的核心 (Machine Learning)**。

我們成功將冷冰冰的 `csv` 數據與模型檔案，轉化為一個溫暖、易用且具備互動性的 Web 應用程式。

我的演示到此結束，謝謝大家。