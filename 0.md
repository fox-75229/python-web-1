## 專案簡報講稿：Flask 心臟病預測平台

（*預計時間：約 3-5 分鐘*）

---

### 1. 開場與專案介紹 (約 45 秒)

大家好，我今天要介紹的專案是「**Flask 心臟病預測平台**」。

這個專案的核心目標是建立一個互動式的 Web 應用程式。我們希望提供一個平台，讓使用者可以輸入相關的健康數據（例如年齡、性別、膽固醇指數等），並透過後端的機器學習模型，即時獲得關於心臟病風險的預測結果。

我們選擇使用 **Flask** 框架來開發，因為它輕量、靈活且易於部署。

### 2. 專案架構與技術棧 (約 45 秒)

在技術實現上，我們的專案結構非常清晰：

* **後端 (`app.py`)**: 這是我們 Flask 應用的核心。它包含了所有的路由定義、處理前端傳來的請求，並負責呼叫機器學習模型進行預測。
* **前端 (`templates` / `static`)**: 我們將 HTML、CSS 和 JavaScript 進行了分離。`templates` 資料夾存放所有 HTML 模板，`static` 則管理所有的靜態資源，確保了前後端的職責分離。
* **環境管理**: 在 Python 環境中，我們使用 `uv` 來管理依賴套件；前端部分則使用 `npm`，確保開發環境的一致性。

### 3. 開發流程與核心功能 (約 60 秒)

我們的開發流程主要分為三個階段：

1.  **數據與模型**: 我們首先對 `heart.csv` 數據集進行了探索性分析 (EDA)。接著，我們訓練了兩種不同的分類模型：**邏輯斯迴歸 (Logistic Regression)** 和 **決策樹 (Decision Tree)**。
2.  **後端開發**: 我們使用 Flask 建立了三個主要路由：一個是專案首頁，另外兩個分別對應邏輯斯迴歸和決策樹的預測介面。
3.  **前端開發**: 我們打造了流暢的使用者介面。使用者在前端填寫表單，JavaScript 會處理提交，並將預測結果動態更新在頁面上。

### 4. 核心亮點：模型比較 (約 90 秒)

本專案最大的亮點，在於我們同時部署了兩種截然不同的模型，並提供了比較。

#### (1) 邏輯斯迴歸 (Logistic Regression)

* 這是一個**線性模型**。
* **優點**: 它的可解釋性非常高。我們可以清楚地知道哪個特徵（比如膽固醇）對預測結果有多大的正面或負面影響。而且它訓練速度快、計算成本低。
* **缺點**: 它只能捕捉線性的關係，對於複雜的模式能力較弱。
* **在本專案中**: 它扮演著一個**穩定且易於解釋的「基準模型」**。

#### (2) 決策樹 (Decision Tree)

* 這是一個**非線性模型**。
* **優點**: 它的決策過程非常直觀，就像一個流程圖，可解釋性也很高。最重要的是，它能夠捕捉到邏輯斯迴歸無法處理的**複雜非線性模式**。
* **缺點**: 它非常容易**過擬合 (Overfitting)**，意思是在訓練資料上表現很好，但在新資料上表現可能不佳。
* **在本專案中**: 它用於探索數據中更複雜的關係，作為線性模型的補充。

### 5. 結論 (約 30 秒)

總結來說，這個專案成功地將兩種機器學習模型部署為一個可互動的 Flask Web 服務。

我們不僅建立了一個實用的預測工具，更重要的是，我們提供了一個平台，讓使用者能直觀地看到兩種不同模型（一個線性、一個非線性）在同一個問題上的應用與差異。

我的報告到此結束，謝謝大家。